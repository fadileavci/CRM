@model IEnumerable<CRM.Entity.Lead>

@{
    ViewBag.Title = "Leads";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Leads</h2>

<p>
	<a href="/Customer/Lead/" class="btn btn-primary">Lead Ekle</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LeadDetail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LeadDetail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @*@Html.ActionLink("Güncelle", "Edit", new { /* id=item.PrimaryKey */ },new { @class="btn btn-success"})*@ 
            @Html.ActionLink("Sil", "DeleteLead", new { id= item.LeadID},new { @class = "btn btn-success" })
        </td>
    </tr>
}

</table>
